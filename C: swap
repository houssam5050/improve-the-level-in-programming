#include <stdio.h>

int main() {
    int x,y,t; 
     printf("enter first number "); 
     scanf("%d",x); 
       printf("enter second number "); 
     scanf("%d",y); 


    t = x;
    x = y;
    y = t;



     printf("after swap x = %d , y = %d ", x, y);




    return 0;
}
